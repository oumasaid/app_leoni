"use strict";

module.exports = {
  async up(queryInterface, Sequelize) {
    // No need to add any commands for altering the table structure
  },

  async down(queryInterface, Sequelize) {
    // No need to add any commands for reverting table alterations
  },
};
